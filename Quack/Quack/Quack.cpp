#include <iostream>
#include <queue>
#include "edx-io.hpp"
using namespace std;


// число отправляем в очередь
void numb(string c) {
	int x = atoi(c);

};

int main()
{
	queue <int> och;
	string c;
	io >> c;
	if (c[0] >= '0' && c[0] <= '9')
		numb(c);
	// берем из очереди, кладем в регистр
	if (c[0] == '>')
	// берем из регистра, кладем в очередь
	if (c[0] == '<')
	// берем два элемента из очереди и выполняем операцию
	if (c[0] == '+')
	if (c[0] == '-')
	if (c[0] == '*')
	if (c[0] == '/')
	if (c[0] == '%')
	if (c[0] == 'Z')
	if (c[0] == 'J')
	if (c[0] == 'E')
	if (c[0] == 'G')
	if (c[0] == 'Q')
	if (c[0] == 'P')
	if (c[0] == 'C')

}


